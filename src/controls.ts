function sendMousePosition() {
  return;
}

function upMouse(offset: number) {
  return offset;
}

function downMouse(offset: number) {
  return offset;
}

function rightMouse(offset: number) {
  return offset;
}

function leftMouse(offset: number) {
  return offset;
}

function drawCircle(radius: number) {
  return radius;
}
function drawSquare(width: number) {
  return width;
}

function drawRectangle(width: number, length: number) {
  return width;
}

function sendPrintScreen() {
  return;
}

export {
  sendMousePosition,
  upMouse,
  downMouse,
  rightMouse,
  leftMouse,
  drawCircle,
  drawSquare,
  drawRectangle,
  sendPrintScreen,
};

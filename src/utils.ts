type parsedMessage = [ string, string | undefined, string | undefined ];

export default function parseMessage(message: string): parsedMessage{
    const command = '';
    const arg1 = '';
    const arg2 = '';
    return [command, arg1, arg2];
}